import { Typography, Container} from "@mui/material";
import React from "react"

function Blogs() {
        return (
            <Container maxWidth="lg" component={"span"}>
            <Typography component={"span"}>
            {/* Under Construction: I am facing a significant dilemma—should I first create a dynamic blogging framework and integrate it here, or should I start by using cards to display static content? Whatever it is, I will soon work on it. */}
            Under development.
            </Typography>
        </Container>
        );
}

export default Blogs