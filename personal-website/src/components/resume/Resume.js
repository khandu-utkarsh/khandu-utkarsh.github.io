import React from "react"
import { Container, Typography } from "@mui/material";
function Resume() {
        return (
            <Container maxWidth="lg" component={"span"}>
            <Typography component={"span"}>
            {/* Under Construction: I will work on this last. I haven't thought about the design yet. I am not looking for anything fancy, like having lots of icons, etc., and making it cluttered. 
            Instead, I'm looking for something plain and simple. If I can use typesetting or markdown, it would be extremely helpful. */}
            Under development.
            </Typography>
        </Container>
        );
}

export default Resume